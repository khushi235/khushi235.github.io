---
# Copy, rename, fill in. Files starting with `_` are ignored by the build.
label: Group Name # e.g. "Languages" — becomes one track map
order: 6 # lower number = shown first
note: One short line describing this group. # optional
items:
  # Either a plain string…
  - Some Technology
  # …or an object, when you want a tooltip or extra emphasis.
  - name: Another Technology
    core: true # true = rendered brighter / larger
    note: Where you actually used it. Never invent years of experience.
---
